server:
  port: 8888

spring:
  application:
    name: jmzhrsEureka
#  security:
#    user:
#      name: admin
#      password: 123456

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

